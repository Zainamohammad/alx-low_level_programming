#include "main.h"
/**
 * mul - function that multiplies two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: returns the result of multiplying the two integers
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

